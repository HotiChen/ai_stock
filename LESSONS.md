# 從 ai_stock 學到的 12 個錯誤

> 這份文件記錄 2026-05 至 2026-09 之間，這套台股 AI 當沖系統在**正式環境**上
> 踩過的錯誤。每一條都是真的發生過、有 commit 或 log 佐證的，不是理論。
>
> **用途**：帶進下一個專案當設計原則的起點。
>
> ---
>
> ## 先講最重要的一件事
>
> 這套系統有 87 個模組、28,677 行程式，以及 114 個測試檔、30,858 行測試。
> 測試比程式還多。
>
> 但它在 2026-08-21 到 2026-09-02 之間**連續 12 天什麼都沒做**，而且沒有任何人
> 發現——log 是乾淨的、process 活著、`pgrep` 找得到、Telegram 沒有異常。
>
> 追出來的四層故障，**沒有一層是「程式寫得亂」造成的**：
>
> | 故障 | 類型 |
> |---|---|
> | 預測寫進錯誤的資料庫 | 一個參數傳錯，log 還印「成功」 |
> | 指標為 None 導致 TypeError | 少一個防護 |
> | HALT 旗標靜默停擺 | 語意設計錯 |
> | 重複的 launchd agent | 根本不在程式碼裡 |
>
> **結論：這個專案不是死於程式品質，是死於「失敗的時候沒有聲音」。**
>
> 重寫一份更漂亮的架構不會解決這件事。新專案要做的第一個功能不是選股、不是
> 下單，是**讓失敗大聲**。

---

## 錯誤 1：成功訊息只說了一半

**症狀**
`logs/main.log` 每天印 `daytrading_db: saved 10 predictions`，但查資料庫永遠 0 筆。
這個矛盾持續了三個多月沒被發現，因為 log 看起來是成功的。

**根因**
寫入目標是可設定的參數，但 log 只印了「幾筆」，沒印「存到哪」。
筆數是對的（真的 insert 了 10 筆），只是進了另一個檔案。

**新專案原則**
> **可設定的目標（路徑、URL、資料表、佇列）必須出現在 log 裡。**
> `saved 10 predictions` 是沒用的；`saved 10 predictions → data/review.db` 才有用。
> 一個不含目標的成功訊息，無法區分「成功」和「成功地做錯事」。

---

## 錯誤 2：兩個語意不同的東西共用一個參數名

**症狀**
`DaytradingDB(db_path)` —— 呼叫端的 `db_path` 指的是 `data/research.db`（研究資料庫），
但 `DaytradingDB` 的預設是 `data/daytrading_review.db`（複盤資料庫）。兩個完全不同
用途的檔案，都叫 `db_path`。

這個 bug 存在於功能誕生的**第一個 commit**（`78430f0`），從來沒有正確運作過。

**新專案原則**
> **命名要能防止誤用。** 兩個不同用途的資料庫不能都叫 `db_path`，
> 要叫 `research_db_path` 和 `review_db_path`。
>
> 更強的做法：不同用途的儲存體用不同型別，讓型別檢查擋下混用。

---

## 錯誤 3：`dict.get(key, default)` 擋不住「存在但為 None」

**症狀**
```
TypeError: '>=' not supported between instances of 'NoneType' and 'float'
```
整份當沖報告在第一支股票就炸掉，當日零預測。

**根因**
```python
volume_ratio = indicators.get("volume_ratio", 1.0)   # 鍵存在、值是 None → 回傳 None
if volume_ratio >= 2.0:                              # 💥
```
`.get()` 的 default 只在**鍵不存在**時生效。外部資料來源（API、爬蟲、資料庫）
給你的是「鍵在、值是 null」。

**新專案原則**
> **所有外部資料一律經過正規化函式，不要直接 `.get()`。**
> ```python
> def num(val, default): return default if val is None else val
> ```
> 這條規則適用於任何跨越系統邊界的 dict：HTTP 回應、資料庫 row、設定檔、
> 第三方 SDK 回傳值。

---

## 錯誤 4：緊急開關把逃生門一起鎖上了 ★ 最危險的一條 ★

**症狀**
`🚨 緊急暫停` 的設計意圖是「停止開新倉」。實作是在主迴圈最頂端：

```python
if is_halted():
    time.sleep(30); continue    # 底下全部跳過
```

於是被擋住的不只有下單，還包括：**停損監控**、**13:00 強制平倉**、收盤複盤。

**如果你抱著當沖部位按下緊急暫停，系統會停止保護那個部位。**
當沖沒平掉，隔天就是違約交割。

**新專案原則**
> **緊急開關只能擋「進」，永遠不能擋「出」。**
> 停止買進、停止開新倉、停止擴大曝險 → 可以。
> 停止停損、停止平倉、停止告警 → 絕對不行。
>
> 實作上：不要把開關放在主迴圈頂端（那是「拔電源」），要放在每一個
> **建立新曝險**的動作之前（那才是「煞車」）。

---

## 錯誤 5：會改變系統行為的狀態，卻不會自我宣告

**症狀**
`data/HALT` 是一個檔案，存在即生效。它：
- 不寫 log
- 不告警
- 不過期
- 不出現在任何狀態查詢裡

2026-08-21 08:25 被按了一次，接下來 12 天系統每天靜靜跳過所有工作。

**新專案原則**
> **任何持久化的、會改變系統行為的狀態，必須在每次啟動時大聲宣告自己。**
>
> - 啟動時：`log.warning` + 推播「系統目前處於 X 狀態」
> - 運行中：定期（每小時）提醒一次，不洗版
> - 一定要有一個地方能回答「系統現在是什麼狀態」——狀態查詢指令要涵蓋它
>
> 判準：**如果一個旗標可以讓系統整天不工作，它就必須每天說一次話。**

---

## 錯誤 6：註解和文案是規格，但沒有人測它

**症狀 A**
```python
"""halt() → write HALT flag, stop MonitorAgent if running"""
def halt(reason="manual"):
    _HALT_FILE.write_text(...)     # 只做了前半，沒有 stop MonitorAgent
```

**症狀 B**
Telegram 顯示「系統已停止，**今日**不再執行任何下單」，
但 `is_halted()` 只看檔案存不存在，是永久生效。
更諷刺的是 `halt()` 早就把日期寫進檔案了 —— `2026-08-21 telegram_manual` ——
只是從來沒有人讀它。

**新專案原則**
> **對使用者說出口的話，就是規格。**
> 文案說「今日」，行為就必須是「今日」。任何一個使用者看得到的承諾
> （提示訊息、按鈕文字、docstring）都應該有一個測試盯著它。
>
> 反過來說：**寫不出測試的承諾，就不要寫進文案。**

---

## 錯誤 7：用技術嚴重度決定 log level，而不是用業務後果

**症狀**
```python
except Exception as e:
    log.warning("當沖預測失敗: %s", e)     # 只是 warning
```
「今日一支當沖候選都沒有」被當成 warning 記下來，數週無人察覺。
從技術角度它確實只是一個 exception；從業務角度它是**整條產品線停擺**。

**新專案原則**
> **log level 由「這件事失敗，使用者會損失什麼」決定，不由 exception 的技術嚴重度決定。**
>
> - 影響今日核心產出 → `log.error(exc_info=True)` + 主動推播
> - 影響輔助功能 → `warning`
> - 可重試、無影響 → `debug`
>
> 具體判準：**如果這個 except 讓今天的核心功能不存在，它就必須主動通知人。**

---

## 錯誤 8：連線成功 ≠ 服務就緒

**症狀**
8:30 選股時 Shioaji 顯示 `connected`，但報價 session 還沒暖機完成
（`pysolace: sol.cpp Not ready`），全市場掃描抓回空清單 → 零候選。
真實模式下沒有 yfinance 備援，於是整天沒有標的。

**新專案原則**
> **就緒探測必須驗證「能取得有效資料」，不能只驗證「連線已建立」。**
> ```python
> # 錯：if api.connected: proceed()
> # 對：探測一支已知標的，拿到 close > 0 才算就緒
> ```
> 而且探測要有**逾時 + 降級 + 告警**三件事：逾時後仍嘗試執行（不比修復前差），
> 但要讓人知道這次是降級跑的。

---

## 錯誤 9：`CREATE TABLE IF NOT EXISTS` 補不上新欄位

**症狀**
`was_correct` 欄位是後來才加進 schema 的。建表語句對**既有資料表**是 no-op，
所以正式環境的資料庫永遠缺這個欄位，`adaptive_scorer` 每天以
`no such column: was_correct` 靜默失敗，長達數月。

**新專案原則**
> **schema 演進必須有明確的 migration 清單，不能靠建表語句。**
> ```python
> _MIGRATIONS = (("table", "column", "TYPE"), ...)   # 新增欄位一併登記在這裡
> ```
> 用 `PRAGMA table_info` 檢查後再 `ADD COLUMN`（可重複執行），
> 不要用 try/except duplicate column。
>
> 補充：既有列的新欄位一律留 NULL，**不要臆測歷史資料的值**。

---

## 錯誤 10：排程有四種說法，其中兩種在打架

**症狀**
同一支 `start_all.sh` 被：
- 腳本註解說要用 `crontab`（實際上 crontab 是空的）
- `com.aistock.start_all.plist`（launchd，8:25，設定正確）
- `com.timchen.quantai.start.plist`（launchd，8:25，**少了 `AbandonProcessGroup`**）
- 使用者手動 `bash start_all.sh`

第三個會在腳本結束時連同 nohup 出來的子程序一起殺掉。兩個 agent 排在同一分鐘，
誰先誰後不固定 —— 一個時好時壞的 race condition。

**新專案原則**
> **排程只能有一個真相來源，而且系統要能回答「誰在排程我」。**
>
> - 啟動時 log 出「我是被誰啟動的」（環境變數、parent PID、或啟動參數）
> - 提供一個 `./doctor` 指令，列出所有排程項目、上次執行時間、上次結束碼
> - 腳本註解裡的排程範例若不是實際使用的機制，就刪掉——它會誤導未來的你

---

## 錯誤 11：只在成功路徑留下資料

**症狀**
當「今日所有股票評分都不到門檻」時，程式早退並回傳「條件不成熟，建議觀望」，
**一筆紀錄都不存**。事後想回答「AI 是不是過濾太嚴」，發現那些天完全沒有痕跡。

同樣地，被篩掉的 42 支股票（評分 < 4）也直接丟棄，無法回答「門檻設 4 分對嗎」。

**新專案原則**
> **「今天沒有結論」也是結論，要記錄下來。**
>
> 決策系統要能回答的不只是「你做了什麼」，還有「你為什麼沒做」。
> 落庫時應該包含：被拒絕的候選、拒絕的理由、當時的門檻值。
>
> 判準：**如果三個月後你想調整某個參數，現在的資料夠不夠讓你判斷？**
> 不夠的話，現在就要開始存。

---

## 錯誤 12：測試會連到正式環境

**症狀**
開發機上有 `.env`（真實的 Shioaji / Telegram / Anthropic 憑證）。
專案多數模組在 import 時呼叫 `load_dotenv(override=True)`，
於是**跑測試會真的連上券商 API、訂閱市場行情**，
造成 macOS 上 `Errno 24: Too many open files`，測試又慢又可能花錢。

**新專案原則**
> **測試必須主動隔離外部憑證，且要在任何專案模組 import 之前執行。**
> ```python
> # conftest.py 最上方，import 專案模組之前
> for k in EXTERNAL_CREDENTIAL_KEYS: os.environ.pop(k, None)
> dotenv.load_dotenv = lambda *a, **k: False   # 壓過模組自己的 load_dotenv
> ```
> 光清 `os.environ` 是不夠的——模組會自己把 `.env` 讀回來。

---

# 新專案的第一週該做什麼

如果只能帶走一件事：**先做可觀測性，再做功能。**

## Day 1–2：讓系統能回答三個問題

| 問題 | 實作 |
|---|---|
| 「你現在是什麼狀態？」 | 一個 `status` 端點/指令，涵蓋所有會改變行為的旗標 |
| 「你今天做了什麼？」 | 每個排程 job 落庫一筆執行紀錄：開始、結束、結果、錯誤 |
| 「你為什麼沒做？」 | 跳過也要記錄，附理由（非交易日 / 已暫停 / 無候選 / 上游失敗） |

## Day 3：心跳與缺席告警

> 排程 job 沒跑，**比跑失敗更難發現**。

每個 job 結束時寫一筆心跳。另外一個獨立的檢查器，在預期時間 +15 分鐘後
檢查心跳是否存在，沒有就告警。**這一個機制就能抓到這次 12 天的靜默停擺。**

## Day 4–5：端到端的煙霧測試

不是單元測試，是「假裝現在是 8:30，完整跑一遍，然後**驗證資料庫真的多了幾筆**」。

錯誤 1 和錯誤 2 都能被這一個測試抓到——因為它會去讀寫入的結果，
而不是相信 log 說的話。

## Day 6–7：才開始搬核心邏輯

技術指標、評分、AI 分析、複盤 —— 這些是舊專案真正的價值，
而且有測試護著，整包搬過來就好。

---

# 帶得走的十條判準

1. 成功訊息要包含「做到哪裡去了」，不只是「做了幾筆」
2. 不同語意的東西不共用參數名
3. 外部資料一律正規化，不直接 `.get()`
4. 緊急開關只擋「進」，不擋「出」
5. 會改變行為的狀態，每次啟動都要說一次話
6. 對使用者說出口的話就是規格，要有測試盯著
7. log level 由業務後果決定，不由技術嚴重度決定
8. 就緒探測要驗證「拿得到有效資料」
9. schema 演進要有 migration 清單
10. 排程只能有一個真相來源，且系統要知道是誰啟動它的
11. 「沒做」也要留紀錄，附理由
12. 測試必須主動隔離外部憑證

---

*整理自 ai_stock 專案 2026-05 至 2026-09 的正式環境故障記錄。*
