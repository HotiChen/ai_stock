// 05.3 回測 — Wave 4-J 完整實作
import AppChrome from '../components/AppChrome';

export default function Simulate() {
  return (
    <AppChrome title="回測" eyebrow="05.3">
      <div style={{ padding: 20, color: 'var(--muted)', fontSize: 12 }}>
        TODO: Wave 4-J 實作回測頁面
      </div>
    </AppChrome>
  );
}
