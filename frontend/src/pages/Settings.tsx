// 06 設定 — Wave 2-E 完整實作
import AppChrome from '../components/AppChrome';

export default function Settings() {
  return (
    <AppChrome title="設定" eyebrow="06">
      <div style={{ padding: 20, color: 'var(--muted)', fontSize: 12 }}>
        TODO: Wave 2-E 實作設定頁面
      </div>
    </AppChrome>
  );
}
