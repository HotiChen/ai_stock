// 05.5 大盤掃描 — Wave 4-J 完整實作
import AppChrome from '../components/AppChrome';

export default function Scanner() {
  return (
    <AppChrome title="大盤掃描" eyebrow="05.5">
      <div style={{ padding: 20, color: 'var(--muted)', fontSize: 12 }}>
        TODO: Wave 4-J 實作大盤掃描頁面
      </div>
    </AppChrome>
  );
}
